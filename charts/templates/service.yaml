apiVersion: v1
kind: Service
metadata:
  name: {{ include "basic-rest-controller.fullname" . }}
  labels:
    {{- include "basic-rest-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "basic-rest-controller.selectorLabels" . | nindent 4 }}
